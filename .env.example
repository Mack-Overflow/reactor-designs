# Local development only. On Render, DATABASE_URL is injected from the
# managed Postgres and VITE_API_URL is composed at build time in render.yaml.

DATABASE_URL=postgres://reactor:reactor@db:5432/reactor_sim
RUST_LOG=info,reactor_api=debug
API_PORT=8080
VITE_API_URL=http://localhost:8080
